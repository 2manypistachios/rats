[build]
  publish = "public"
  command = "npm run build"
[build.environment]
  YARN_VERSION = "1.3.2"
  YARN_FLAGS = "--no-ignore-optional"

[[redirects]]
  # Redirect default Netlify subdomain to primary domain
  https://taleoftheserpent.netlify.com/* https://www.tots.cc/:splat 301!